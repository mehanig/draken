# Draken Configuration

# Claude Authentication (choose one):
# Option 1 (Recommended): Run `claude login` to authenticate with your Anthropic account
# Option 2: Set your API key below
# ANTHROPIC_API_KEY=your-anthropic-api-key

# Web Authentication (required for deployment, optional for local use)
# If not set, run with DRAKEN_NO_AUTH=true for local development
DRAKEN_USERNAME=admin
DRAKEN_PASSWORD=your-secure-password
DRAKEN_JWT_SECRET=your-random-secret-key-at-least-32-chars

# Token expiry (optional, default: 7d)
DRAKEN_TOKEN_EXPIRY=7d

# Disable authentication for local development (NOT for production)
# DRAKEN_NO_AUTH=true
